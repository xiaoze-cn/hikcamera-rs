# HikCamera MVS bindings are generated by bindgen via build.rs, which passes
# `-I include/` to libclang. clangd does not see build.rs, so we mirror that
# flag here for editor/DAP support when browsing `wrapper.h` and the SDK headers.
CompileFlags:
  Add: [-Iinclude]
