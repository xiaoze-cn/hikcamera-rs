# hikcamera-rs documentation site

This site is built with Astro + Starlight.

## Local development

From the repository root:

```sh
just site-install
just site-dev
```

Or from `site/` directly:

```sh
just install
just dev
```

## Build

```sh
just site-build
```
